import React from 'react';

const Loader = () => (
    <strong>
        <span>Loading</span>
        <span>.</span>
        <span>.</span>
        <span>.</span>
    </strong>
);

export default Loader;
