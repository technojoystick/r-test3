export const ReduxNamespaces = {
    LIST: 'list',
} as const;

export const RequestStatuses = {
    INACTIVE: 'inactive',
    REQUEST: 'request',
    SUCCESS: 'success',
    FAILURE: 'failure'
} as const;
